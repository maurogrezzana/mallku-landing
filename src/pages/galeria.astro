---
import BaseLayout from '../layouts/BaseLayout.astro';
import Header from '../components/Header.astro';
import Footer from '../components/Footer.astro';
import WhatsAppFloat from '../components/WhatsAppFloat.astro';
import { whatsappLinks } from '../data/site';

const galeriaItems = [
  {
    categoria: 'Quilmes',
    items: [
      { titulo: 'Ciudad Sagrada de Quilmes', src: '/images/Quilmes 1.jpg' },
      { titulo: 'Ruinas de Quilmes', src: '/images/Quilmes 2.jpg' },
      { titulo: 'Quilmes - Vista panorámica', src: '/images/quilmes 3.jpg' },
      { titulo: 'Quilmes - Terrazas', src: '/images/quilmes 4.jpg' },
      { titulo: 'Quilmes - Sendero', src: '/images/quilmes 5.jpg' },
      { titulo: 'Quilmes - Muros ancestrales', src: '/images/Quilmes 6.jpg' },
      { titulo: 'Quilmes - Paisaje', src: '/images/Quilmes 7.jpg' },
    ]
  },
  {
    categoria: 'Tafí y Menhires',
    items: [
      { titulo: 'Menhires - Monolitos', src: '/images/menhires 1.jpg' },
      { titulo: 'Menhires - Detalle', src: '/images/menhires 2.jpg' },
      { titulo: 'Menhires - Parque', src: '/images/menhires 3.jpg' },
      { titulo: 'Menhires - Vista general', src: '/images/Menhires 4.jpg' },
      { titulo: 'Tafí del Valle', src: '/images/Tafi del Valle.jpg' },
      { titulo: 'Abra del Infiernillo', src: '/images/Infiernillo.jpg' },
    ]
  },
  {
    categoria: 'Tucumán Colonial',
    items: [
      { titulo: 'Ruinas de Ibatín', src: '/images/Ibatin 1.jpg' },
      { titulo: 'Ibatín - Excavación', src: '/images/ibatin 2.jpg' },
      { titulo: 'Ibatín - Restos', src: '/images/Ibatin 3.jpg' },
      { titulo: 'Ibatín - Sitio', src: '/images/Ibatin 4.jpg' },
      { titulo: 'Ibatín - Vista', src: '/images/Ibatin 5.jpg' },
      { titulo: 'Museo Jesuita', src: '/images/Museo jesuita 1.jpg' },
      { titulo: 'Museo Jesuita - Interior', src: '/images/museo jesuita 2.jpg' },
      { titulo: 'Museo Jesuita - Detalle', src: '/images/museo jesuita 3.jpg' },
      { titulo: 'Reducción de Lules', src: '/images/reduccion lules 1.jpg' },
      { titulo: 'Reducción de Lules - Vista', src: '/images/reduccion lules 2.jpg' },
      { titulo: 'Reducción de Lules - Detalle', src: '/images/reduccion lules 3.jpg' },
      { titulo: 'Reducción de Lules - Paisaje', src: '/images/reduccion lules 4.jpg' },
    ]
  },
  {
    categoria: 'Puna y Experiencia 4x4',
    items: [
      { titulo: 'Antofagasta de la Sierra', src: '/images/ANS.jpg' },
      { titulo: 'ANS - Paisaje', src: '/images/ANS 6.jpg' },
      { titulo: 'ANS - Camino', src: '/images/ANS 9.jpg' },
      { titulo: 'ANS - Vista panorámica', src: '/images/ANS 12.jpg' },
      { titulo: 'ANS - Formaciones', src: '/images/ANS 15.jpg' },
      { titulo: 'ANS - Horizonte', src: '/images/ANS 18.jpg' },
      { titulo: 'Confluencia', src: '/images/Confluencia 1.JPG' },
      { titulo: 'Confluencia - Río', src: '/images/Confluencia 3.JPG' },
      { titulo: 'Real Grande', src: '/images/real grande.jpg' },
      { titulo: 'Real Grande - Paisaje', src: '/images/Real Grande 5.jpg' },
      { titulo: 'Real Grande - Valle', src: '/images/Real Grande 9.jpg' },
      { titulo: 'Real Grande - Montañas', src: '/images/Real Grande 13.jpg' },
      { titulo: 'Dunas', src: '/images/Dunas.JPG' },
      { titulo: 'Campo las Tobas', src: '/images/Campo las tobas 1.jpg' },
      { titulo: 'Pucará los Negros', src: '/images/Pucara los Negros 1.jpg' },
      { titulo: 'Experiencia 4x4', src: '/images/4x4.png' },
      { titulo: '4x4 - Aventura', src: '/images/4x4 2.png' },
      { titulo: '4x4 - Puna', src: '/images/4x4 3.png' },
    ]
  }
];
---

<BaseLayout
  title="Galería"
  description="Fotos de nuestras excursiones arqueológicas: Quilmes, Tafí del Valle, menhires, paisajes del NOA y más. Descubrí los destinos que vas a recorrer."
>
  <WhatsAppFloat />
  <Header />

  <main>
    <!-- HERO -->
    <section class="hero-galeria" style="background-image: linear-gradient(to bottom, rgba(6,27,56,0.6), rgba(6,27,56,0.85)), url('/images/ANS 15.jpg');">
      <div class="wrap text-center">
        <h1>Galería de <span class="accent">Imágenes</span></h1>
        <p class="hero-lead">
          Los paisajes, sitios arqueológicos y momentos que vas a vivir en nuestras excursiones.
        </p>
      </div>
    </section>

    <!-- GALERÍA POR CATEGORÍAS -->
    {galeriaItems.map((seccion) => (
      <section class="section section-dark">
        <div class="wrap">
          <h2 class="categoria-title">{seccion.categoria}</h2>
          <div class="galeria-grid">
            {seccion.items.map((item) => (
              <div class="galeria-item" data-lightbox>
                <div class="galeria-imagen">
                  <img src={item.src} alt={item.titulo} loading="lazy" />
                </div>
                <div class="galeria-info">
                  <h3>{item.titulo}</h3>
                </div>
              </div>
            ))}
          </div>
        </div>
      </section>
    ))}

    <!-- NOTA -->
    <section class="section section-light">
      <div class="wrap text-center">
        <div class="nota-box">
          <h3>¿Querés ver más?</h3>
          <p>
            Seguinos en Instagram donde compartimos fotos de cada salida, historias de los sitios que visitamos y novedades de próximas excursiones.
          </p>
          <a class="btn btn-primary" href="https://instagram.com/mallkuexcursiones" target="_blank" rel="noopener">
            @mallkuexcursiones
          </a>
        </div>
      </div>
    </section>

    <!-- CTA -->
    <section class="section section-terracota">
      <div class="wrap text-center">
        <h2 class="section-title">¿Te gustaría estar ahí?</h2>
        <p class="section-subtitle" style="color: rgba(255,255,255,0.85);">
          Consultá fechas disponibles y reservá tu lugar en la próxima salida.
        </p>
        <a class="btn" href={whatsappLinks.fechas} target="_blank" rel="noopener" style="background: #fff; color: var(--terracota);">
          Consultar por WhatsApp
        </a>
      </div>
    </section>
  </main>

  <!-- LIGHTBOX -->
  <div id="lightbox" class="lightbox" style="display: none;">
    <button class="lightbox-close" aria-label="Cerrar">&times;</button>
    <button class="lightbox-prev" aria-label="Anterior">&#8249;</button>
    <button class="lightbox-next" aria-label="Siguiente">&#8250;</button>
    <img id="lightbox-img" src="" alt="" />
    <p id="lightbox-caption" class="lightbox-caption"></p>
  </div>

  <Footer />
</BaseLayout>

<style>
  .hero-galeria {
    padding: 80px 0 40px;
    background-size: cover;
    background-position: center;
  }

  .hero-galeria h1 {
    margin: 0 0 20px;
    font-size: clamp(40px, 6vw, 56px);
  }

  .hero-galeria .accent {
    color: var(--color-primary);
  }

  .hero-lead {
    font-size: 18px;
    color: rgba(239, 227, 214, 0.8);
    max-width: 60ch;
    margin: 0 auto;
  }

  /* Categorías */
  .categoria-title {
    font-size: 28px;
    margin: 0 0 var(--space-lg);
    padding-bottom: var(--space-sm);
    border-bottom: 2px solid var(--color-primary);
    display: inline-block;
  }

  /* Galería Grid */
  .galeria-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: var(--space-md);
  }

  .galeria-item {
    border-radius: var(--radius-lg);
    overflow: hidden;
    background: rgba(239, 227, 214, 0.05);
    border: 1px solid var(--color-line-light);
    transition: transform var(--transition-normal), box-shadow var(--transition-normal);
    cursor: pointer;
  }

  .galeria-item:hover {
    transform: translateY(-4px);
    box-shadow: 0 12px 40px rgba(0, 0, 0, 0.3);
  }

  .galeria-imagen {
    aspect-ratio: 4/3;
    overflow: hidden;
  }

  .galeria-imagen img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    transition: transform 0.4s ease;
  }

  .galeria-item:hover .galeria-imagen img {
    transform: scale(1.08);
  }

  .galeria-info {
    padding: var(--space-sm) var(--space-md);
  }

  .galeria-info h3 {
    margin: 0;
    font-size: 13px;
    text-transform: uppercase;
    letter-spacing: 0.5px;
  }

  /* Nota Box */
  .nota-box {
    max-width: 600px;
    margin: 0 auto;
    padding: var(--space-xl);
    background: #fff;
    border: 2px solid var(--color-primary);
    border-radius: var(--radius-xl);
  }

  .nota-box h3 {
    margin: 0 0 var(--space-sm);
    font-size: 24px;
    color: var(--azul);
  }

  .nota-box p {
    margin: 0 0 var(--space-lg);
    color: var(--marron);
  }

  /* Lightbox */
  .lightbox {
    position: fixed;
    inset: 0;
    background: rgba(0, 0, 0, 0.92);
    z-index: 9999;
    display: flex;
    align-items: center;
    justify-content: center;
    flex-direction: column;
  }

  .lightbox img {
    max-width: 90vw;
    max-height: 80vh;
    object-fit: contain;
    border-radius: 8px;
  }

  .lightbox-caption {
    color: #fff;
    font-size: 16px;
    margin-top: 16px;
    text-align: center;
  }

  .lightbox-close {
    position: absolute;
    top: 20px;
    right: 30px;
    background: none;
    border: none;
    color: #fff;
    font-size: 48px;
    cursor: pointer;
    line-height: 1;
    z-index: 10;
  }

  .lightbox-prev,
  .lightbox-next {
    position: absolute;
    top: 50%;
    transform: translateY(-50%);
    background: rgba(255, 255, 255, 0.15);
    border: none;
    color: #fff;
    font-size: 48px;
    cursor: pointer;
    padding: 8px 16px;
    border-radius: 8px;
    z-index: 10;
    transition: background 0.2s;
  }

  .lightbox-prev:hover,
  .lightbox-next:hover {
    background: rgba(255, 255, 255, 0.3);
  }

  .lightbox-prev {
    left: 20px;
  }

  .lightbox-next {
    right: 20px;
  }

  @media (max-width: 1100px) {
    .galeria-grid {
      grid-template-columns: repeat(3, 1fr);
    }
  }

  @media (max-width: 768px) {
    .galeria-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 480px) {
    .galeria-grid {
      grid-template-columns: 1fr;
    }

    .hero-galeria {
      padding: 60px 0 30px;
    }

    .lightbox-prev,
    .lightbox-next {
      font-size: 32px;
      padding: 4px 12px;
    }
  }
</style>

<script>
  const lightbox = document.getElementById('lightbox')!;
  const lightboxImg = document.getElementById('lightbox-img') as HTMLImageElement;
  const lightboxCaption = document.getElementById('lightbox-caption')!;
  const items = document.querySelectorAll('[data-lightbox]');

  let currentIndex = 0;
  const allImages: { src: string; title: string }[] = [];

  items.forEach((item) => {
    const img = item.querySelector('img');
    const title = item.querySelector('h3');
    if (img) {
      allImages.push({
        src: img.getAttribute('src') || '',
        title: title?.textContent || ''
      });
    }
  });

  function openLightbox(index: number) {
    currentIndex = index;
    lightboxImg.src = allImages[index].src;
    lightboxImg.alt = allImages[index].title;
    lightboxCaption.textContent = allImages[index].title;
    lightbox.style.display = 'flex';
    document.body.style.overflow = 'hidden';
  }

  function closeLightbox() {
    lightbox.style.display = 'none';
    document.body.style.overflow = '';
  }

  function navigate(dir: number) {
    currentIndex = (currentIndex + dir + allImages.length) % allImages.length;
    lightboxImg.src = allImages[currentIndex].src;
    lightboxImg.alt = allImages[currentIndex].title;
    lightboxCaption.textContent = allImages[currentIndex].title;
  }

  items.forEach((item, i) => {
    item.addEventListener('click', () => openLightbox(i));
  });

  document.querySelector('.lightbox-close')?.addEventListener('click', closeLightbox);
  document.querySelector('.lightbox-prev')?.addEventListener('click', () => navigate(-1));
  document.querySelector('.lightbox-next')?.addEventListener('click', () => navigate(1));

  lightbox.addEventListener('click', (e) => {
    if (e.target === lightbox) closeLightbox();
  });

  document.addEventListener('keydown', (e) => {
    if (lightbox.style.display === 'none') return;
    if (e.key === 'Escape') closeLightbox();
    if (e.key === 'ArrowLeft') navigate(-1);
    if (e.key === 'ArrowRight') navigate(1);
  });
</script>
